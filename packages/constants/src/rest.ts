import { version } from "@spall/core";

export const API_VERSION = 10;
export const BASE_URL = "https://discord.com/api";
export const CDN_URL = "https://cdn.discordapp.com";

export const DEFAULT_USER_AGENT = `DiscordBot (https://github.com/spalljs/spall, ${version})`;
export const DEFAULT_REST_TIMEOUT = 15000;
export const DEFAULT_REST_OFFSET = 500;

export const RATE_LIMIT_PRECISION = 1000;
